- name: Test Connectivity
  shell: bash
  env:
    TOKEN_URL: ${{ secrets.CPI_TOKEN_URL }}
  run: |
    set -e

    echo "Testando DNS..."
    nslookup 386ab0fbtrial.authentication.us10.hana.ondemand.com || true

    echo "Testando CURL verbose..."
    curl -v "$TOKEN_URL" || true
